[project]
name = "resume"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyicu>=2.14",
    "tzlocal>=5.3",
]

[dependency-groups]
dev = [
    "ruff>=0.9.9",
]
